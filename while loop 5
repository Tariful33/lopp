#include <stdio.h>

int main() 
{
    int a = 48, b = 18;
    while (a != b) {
        if (a > b)
            a = a - b;
        else
            b = b - a;
    }
    printf("The GCD is: %d", a);
    return 0;
}
